import 'package:flutter/material.dart';

/// Base class for all settings sections.
///
/// Extend this to create a fully custom section widget that can be placed
/// inside a [SettingsList].
abstract class AbstractSettingsSection extends StatelessWidget {
  const AbstractSettingsSection({super.key});
}
