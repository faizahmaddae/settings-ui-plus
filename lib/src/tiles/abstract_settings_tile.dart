import 'package:flutter/material.dart';

/// Base class for all settings tiles.
///
/// Extend this to create a fully custom tile widget that can be placed
/// inside a [SettingsSection].
abstract class AbstractSettingsTile extends StatelessWidget {
  const AbstractSettingsTile({super.key});
}
