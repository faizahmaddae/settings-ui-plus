name: settings_ui_plus
description: "Build native-looking settings screens in minutes. A single API that adapts to Material and Cupertino design guidelines automatically."
version: 0.2.7
repository: https://github.com/faizahmaddae/settings-ui-plus
topics:
  - settings
  - ui
  - preferences

environment:
  sdk: ^3.11.0
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

